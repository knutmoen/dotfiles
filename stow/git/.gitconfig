# --------------------------------------------------
# Global Git configuration (safe to commit)
# --------------------------------------------------

[core]
  editor = nvim
  excludesfile = ~/.gitignore_global

[pull]
  rebase = true

[init]
  defaultBranch = main

[color]
  ui = auto

# --------------------------------------------------
# Dotfiles repo
# --------------------------------------------------

[includeIf "gitdir:/Users/knselo/dotfiles/**"]
  path = ~/.gitconfig-personal

[includeIf "gitdir:/Users/knselo/dotfiles/**"]
  path = ~/.gitconfig-local-personal

# --------------------------------------------------
# Work repos
# --------------------------------------------------

[includeIf "gitdir:/Users/knselo/development/projects/**"]
  path = ~/.gitconfig-work

[includeIf "gitdir:/Users/knselo/development/projects/**"]
  path = ~/.gitconfig-local-work

# --------------------------------------------------
# Personal repos
# --------------------------------------------------

[includeIf "gitdir:/Users/knselo/development/privat/**"]
  path = ~/.gitconfig-personal

[includeIf "gitdir:/Users/knselo/development/privat/**"]
  path = ~/.gitconfig-local-personal
