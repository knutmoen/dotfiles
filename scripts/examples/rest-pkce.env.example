# Eksempel på lokal konfig for PKCE-skriptene.
# Kopier til ~/.rest-pkce.env eller ~/.config/rest-pkce.env og fyll inn egne verdier.
#
# OKTA_ISSUER: https://<okta-domene>/oauth2/<auth-server-id>
# CLIENT_ID:   Klient-ID for appen i Okta
# USERNAME:    Brukernavn for brukerkontoen som skal logge inn
# PASSWORD:    Passord for brukerkontoen
# SCOPE:       Scopes som kreves av API (f.eks. "openid groups profile")
# REDIRECT_URI:Må matche en whitelisted redirect i Okta-klienten (f.eks. https://app.example.com/login/callback)
# CACHE_FILE:  (valgfritt) plassering av token-cache
# SKEW_SECONDS:(valgfritt) hvor tidlig vi fornyer før utløp

OKTA_ISSUER="https://your-okta-domain/oauth2/your-auth-server-id"
CLIENT_ID="your-client-id"
USERNAME="your-username"
PASSWORD="your-password"
SCOPE="openid groups profile"
REDIRECT_URI="https://yourapp.example.com/login/callback"

# Optional overrides
# CACHE_FILE="$HOME/.cache/rest-pkce-token.json"
# SKEW_SECONDS=60
